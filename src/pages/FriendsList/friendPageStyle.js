import {StyleSheet} from 'react-native';

export const fpageStyle = StyleSheet.create({
  textWrap: {
    width: '100%',
    padding: 8,
    paddingLeft: 15,
    backgroundColor: '#fff',
    marginTop: 0,
  },
});
